import { Routes } from '@angular/router';

/**
 * PUBLIC_INTERFACE
 * App routes. Currently single-page; preserved for future extensibility.
 */
export const routes: Routes = [
  // Future routes can be added here
];
